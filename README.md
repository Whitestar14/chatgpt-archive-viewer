# ChatGPT Archive Viewer

A simple viewer built in React with full markdown and LaTeX support for visualizing chatgpt data exports on a web browser. Data is imported with JSON, conventionally found as `conversation.json` in the zip data archive you extracted from.